fn generate_sprite_struct(sprite: &SpriteWithPalette, palette_mapper: &palette::PaletteMapper) {
    // struct needs to hold:
    //  palbank index
    //  tile data (4BPP pixels, indices into the palbank)
    // idk other stuff probably (size maybe?)
}